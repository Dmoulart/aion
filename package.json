{
  "name": "aion",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "sandbox": "cd sandbox && bun run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-types": "latest",
    "@types/jest": "^29.2.4",
    "ts-jest": "^29.0.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}