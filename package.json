{
  "name": "aion",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "sandbox": "cd sandbox && bun run dev",
    "test": "vitest run",
    "watch:ecs": "cd ./packages/ecs && bun run watch",
    "watch:net": "cd ./packages/net && bun run watch",
    "watch:render": "cd ./packages/render && bun run watch",
    "watch:core": "cd ./packages/core && bun run watch",
    "watch:input": "cd ./packages/input && bun run watch",
    "watch:engine": "cd ./packages/engine && bun run watch",
    "watch:preset": "cd ./packages/preset && bun run watch"
  },
  "workspaces": ["packages/*", "sandbox"],
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
