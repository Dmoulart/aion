{
  "name": "aion",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "sandbox": "cd sandbox && bun run dev",
    "test": "vitest run"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}