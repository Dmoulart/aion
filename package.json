{
  "name": "aion",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "sandbox": "cd sandbox && bun run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}